# React Concepts 

## Creating a React app using Vite: (Vite is faster and gives us smaller bundle sizes)
1. npm create vite@latest
2. npm install

## Create React App without using Vite
1. Create react app
2. npm install
